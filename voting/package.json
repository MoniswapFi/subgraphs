{
    "name": "v1.0.0",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ moniswap-bartio-voting/v1.0.0",
        "create-local": "graph create --node http://localhost:8020/ moniswap-bartio-voting/v1.0.0",
        "remove-local": "graph remove --node http://localhost:8020/ moniswap-bartio-voting/v1.0.0",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 moniswap-bartio-voting/v1.0.0",
        "test": "graph test"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.78.0",
        "@graphprotocol/graph-ts": "0.32.0"
    },
    "devDependencies": {
        "matchstick-as": "0.5.0"
    }
}
