{
  "name": "pairs",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --network mainnet --ipfs https://graph.vefinetwork.org/app/ipfs/ --node https://graph.vefinetwork.org/app/bitgert/admin/ vefi/exchange",
    "create-remote": "graph create --network mainnet --node https://graph.vefinetwork.org/app/bitgert/admin/ vefi/exchange",
    "create-local": "graph create --node http://localhost:8020/ Factory",
    "remove-local": "graph remove --node http://localhost:8020/ Factory",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Factory",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.70.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
